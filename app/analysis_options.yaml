include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - build/**
    - android/**
    - web/**
    - windows/**
  language:
    strict-casts: true

linter:
  rules:
    prefer_single_quotes: true
    avoid_print: true
